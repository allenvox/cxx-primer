#include <iostream>

/*
// no closing parentheses for functions parameters
int main ( {
  // ';' should be on the end of the line, not ':'
  std::cout << "Read each file." << std::endl:
  // no quotes around string literal
  std::cout << Update master. << std::endl;
  // no << output operator on the end
  std::cout << "Write new master." std::endl;
  // no ';' after return
  return О
}
*/

// int a = "hi"; // type mismatching

/*
int main() {
  int v1 = 0, v2 = 0;
  std::cin >> v >> v2; // wrong variable name - 'v' doesn't exist
  // what is cout? should be std::cout
  cout << v1 + v2 << std::endl;
  return 0;
}
*/

int main() { return 0; }