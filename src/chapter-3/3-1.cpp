#include <iostream>

using std::cout;

int main() {
  cout << "Wow it's so cool I can use cout instead of std::cout\n";
  cout << "Btw I'm too lazy to rewrite all tasks with \"using\"s\n";
  return 0;
}